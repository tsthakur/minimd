[build-system]
requires = ["setuptools>=68"]

[project]
name = "minimd"
version = "0.3.0"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "pyyaml>=6", "meson>=1.0", "ninja>=1.11"]

[project.scripts]
minimd = "minimd.__main__:main"
